extends layout.pug

block content

  nav.grid-container.portfolio-breadcrumb
    a(href='/') ← Back
    
      .error-page
        h1 |  Sorry, we've had an error: #{error.message}
        h2 Status Code: #{error.status}
        pre= error.stack